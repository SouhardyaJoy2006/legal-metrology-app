# Full local/dev dependency set, including the CLIP category classifier.
# NOT used for Vercel deploys — see requirements.txt (Vercel's build reads
# that file, and torch/torchvision/CLIP are too large for its function size
# limit). ai_services.py already catches missing torch/CLIP gracefully, so
# running with requirements.txt on Vercel just skips category
# classification instead of crashing.
#
# Install this file locally to get category classification working:
#   pip install -r requirements-full.txt
blinker==1.9.0
Bootstrap-Flask==2.2.0
click==8.4.2
colorama==0.4.6
Flask==2.3.2
Flask-CKEditor==0.4.6
Flask-Gravatar==0.5.0
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
greenlet==3.5.3
gunicorn==25.1.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.2
sqlalchemy-libsql
python-dotenv==1.2.2
SQLAlchemy==2.0.36
typing_extensions==4.16.0
Werkzeug==2.3.7
WTForms==3.0.1
# --- AI pipeline: category classifier (CLIP) + label extractor (Gemini) ---
torch
torchvision
ftfy
regex
tqdm
email_validator
git+https://github.com/openai/CLIP.git
google-genai
Pillow
requests
vercel_blob
fpdf
